# Team View

Shows you where your teammates are on the HUD.

## Integration

### How to toggle the mod

```java
public void enableTeamView(Player viewer, boolean value) {
    ModTeamView.ENABLED.set(viewer, value);
}
```

## Available options

- __`ENABLED`__
    - Values
        - Type: `Boolean`
        - Default: `true`

